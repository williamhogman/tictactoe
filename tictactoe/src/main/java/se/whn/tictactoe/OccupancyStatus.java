package se.whn.tictactoe;

/**
 * Occupancy status refers to the status of a square as viewed by a an
 * agent, that if is occupied by itself, the opponent or is empty.
 */
public enum OccupancyStatus {
    Me,
    Opponent,
    Empty,
}
